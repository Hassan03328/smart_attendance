name: smart_attendance_app
description: Smart Attendance App with QR and Location
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.15.2
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0

  provider: ^6.0.5
  shared_preferences: ^2.5.3

  mobile_scanner: ^7.1.2
  qr_flutter: ^4.1.0

  geolocator: ^10.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
